#include <stdio.h>
int main()
{
	int prime[50000] = {}, target, max = 0, j, find = 2;
	prime[0] = 2;
	while (find < target && find <= 100000)
	{
		next:
			find++;
		j = i;
		while (j >= 0)
		{
			
			j--;
		}
	}
	return 0;
}
